new VenoBox({
    selector: '.my-image-links',
    numeration: true,
    infinigall: true,
    share: true,
    spinner: 'wave',
   
});

new VenoBox({
    selector: '.my-custom-links',
    spinner: 'wave',
});